# Data-tier password — must EQUAL POST_POSTGRES_PASSWORD in env/prod/infra/.env.
# Delivered out-of-band (PGPASSWORD/REDIS_PASSWORD) — any characters are fine. openssl rand -base64 30
POST_POSTGRES_PASSWORD=change-me
