# Data-tier password — must EQUAL STATS_REDIS_PASSWORD in env/prod/infra/.env.
# Delivered out-of-band (PGPASSWORD/REDIS_PASSWORD) — any characters are fine. openssl rand -base64 30
STATS_REDIS_PASSWORD=change-me
