# Data-tier password — must EQUAL STORE_POSTGRES_PASSWORD in env/prod/infra/.env.
# Delivered out-of-band (PGPASSWORD/REDIS_PASSWORD) — any characters are fine. openssl rand -base64 30
STORE_POSTGRES_PASSWORD=change-me
