# Fill by hand — intervals + the monitored-service list are baked into
# docker-compose.yml.
TELEGRAM_BOT_TOKEN=123456:ABC-your-bot-token
TELEGRAM_CHAT_ID=-1001234567890

# Only if api.telegram.org is blocked from your host: point the Bot API at a
# Cloudflare Worker reverse proxy (see cloudflare-worker.js + README here).
#TELEGRAM_API_BASE=https://pmapa-tg.<your-subdomain>.workers.dev
