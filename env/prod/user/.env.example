# On-box data tier (env/prod/infra); keep the password in sync with infra/.env.
DATABASE_URL=postgres://user:change-me@user-postgres:5432/user?sslmode=disable

# Optional — comma-separated moderator user ids (unlocks the Moderation queue).
#USER_ADMIN_IDS=
