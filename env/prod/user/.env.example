# Fill by hand — everything else is baked into docker-compose.yml.
DATABASE_URL=postgres://user:CHANGE-ME@user-db.internal:5432/user?sslmode=require

# Optional — comma-separated moderator user ids (unlocks the Moderation queue).
#USER_ADMIN_IDS=
